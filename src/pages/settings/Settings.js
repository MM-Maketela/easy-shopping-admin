import React from 'react'
import { MiniBar } from '../../components/mini-bar/MiniBar'

export const Settings = () => {
  return (
    <div>
    <MiniBar  info={{title:"settings"}}/>
    <div>
        settings
    </div>

    </div>
  )
}
